{
  "name": "nanhuaijin-rag",
  "version": "2.0.0",
  "private": true,
  "description": "南怀瑾著作服务端检索与证据约束问答",
  "scripts": {
    "dev": "npx vercel dev",
    "db:import": "node scripts/import-db.mjs",
    "corpus:audit": "node scripts/audit-corpus.mjs --strict",
    "retrieval:eval": "node scripts/eval-retrieval.mjs",
    "test": "node --test tests/*.test.js tests/*.test.mjs",
    "check": "node --check app.js && node --check api/search.mjs && node --check api/health.mjs && node --check server/database.mjs && node --check server/search-core.mjs && node --check scripts/import-db.mjs && node --check scripts/audit-corpus.mjs && node --check scripts/eval-retrieval.mjs"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  }
}
